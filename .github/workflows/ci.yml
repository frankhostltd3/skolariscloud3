name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup basics
        run: echo "No build steps yet"
      - name: Lint placeholder
        run: echo "No linters configured"
      - name: Test placeholder
        run: echo "No tests configured"
